version: '3.8'

services:
  mizan-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
    volumes:
      - ./data:/app/data
    restart: always
